<% provide :title, " | 新規説明会登録" %>
<div class="form_wrapper wf-roundedmplus1c">
  <%= form_for(@briefing) do |f| %>
  <h2 class="form_title">新規説明会登録</h2>
  <p class="center-align">※「<span class="red-text">＊</span>」は必須項目</p>
  <%= render partial: 'layouts/flash' %>
  <div class="row">
    <div class="col s12">
            <div class="row">
        <div class="col s6">
            <%= f.label :"拠点＊" %>
            <%= f.collection_select :base_id, @bases, :id, :name, { prompt: '選択してください' }, {class: "browser-default"} %>
        </div>
        <div class="col s6">
          <p>担当メンター名<span class="red-text">＊</span></p>
            <%= f.text_field :mentor %>
        </div>
      </div>
      <div class="row">
        <div class="input-field col s6">
          <p>日にち<span class="red-text">＊</span></p>
          <%= f.datetime_field :schedule_at_date, class: "datepicker", placeholder: "クリックして選択してください" %>
        </div>
        <div class="input-field col s6">
          <p>時間<span class="red-text">＊</span></p>
          <%= f.datetime_field :schedule_at_time, class: "timepicker", placeholder: "クリックして選択してください" %>
        </div>
      </div>
      <div class="row">
        <div id="submit">
          <%= f.submit "送信",id: "submit_button", data: { disable_with: "送信中..." }, class: "btn waves-effect waves-light" %>
        </div>
      </div>
      <br>
    </div>
  </div>
  <% end %>
</div>