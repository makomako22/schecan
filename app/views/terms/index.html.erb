<% provide :title, " | 受講期一覧" %>
<h2 class="form_title">受講期一覧</h2>
<p class="center-align">※編集・削除を希望の方は@makoto-arakiまで</p>
<%= render partial: 'layouts/flash' %>
<div class="row">
  <div class="input-field col s12">
    <table class="t_border striped centered">
      <thead>
        <tr>
          <th>期間</th>
          <th>受講生数</th>
          <th>受講生一覧</th>
          <th>新規受講生登録</th>
        </tr>
      </thead>
      <tbody>
        <% @terms.each do |term| %>
        <tr>
          <td><%= term.term_t %></td>
          <td><%= term.student_count(term) %></td>
          <td><%= link_to "一覧", term_path(term.id) %></td>
          <td><%= link_to "#{term.term_i}期で登録", new_student_path(term: term.term_i) %></td>
        </tr>
        <% end %>
      </tbody>
    </table>
    <%= paginate(@terms) %>
  </div>
</div>
